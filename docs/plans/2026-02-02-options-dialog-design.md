# 选项对话框与帮助菜单重构设计

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标:** 将 JLab 的设置项统一到选项对话框，重构帮助菜单符合 Windows 惯例。

**架构:** 新增选项对话框（多标签页）和关于对话框，重构文件菜单和帮助菜单结构。

**技术栈:** Rust, egui

---

## 概述

### 当前问题
- 快捷键说明直接显示在帮助菜单中（占用大量空间）
- 快捷键设置是独立窗口
- 语言选择在帮助菜单的子菜单中
- 帮助菜单不符合 Windows 应用惯例

### 新设计
1. **选项对话框**（文件菜单）- 统一管理所有设置
2. **帮助菜单重构** - 帮助、检查更新、关于

---

## 功能设计

### 1. 选项对话框

**位置:** 文件菜单 → "选项..."

**标签页:**
- **常规** - 语言选择、自动保存开关
- **快捷键** - 复用现有快捷键设置界面

**UI 布局:**
```
┌─────────────────────────────────────┐
│  选项                            × │
├─────────────────────────────────────┤
│ [常规] [快捷键]                     │
├─────────────────────────────────────┤
│  常规标签内容:                       │
│  ┌─────────────────────────────┐   │
│  │ 界面语言: [中文] ▼          │   │
│  ├─────────────────────────────┤   │
│  │ ☑ 自动保存                  │   │
│  └─────────────────────────────┘   │
│                                     │
│  快捷键标签页:                       │
│  (复用现有 ShortcutEditorState)     │
│                                     │
├─────────────────────────────────────┤
│            [确定] [取消]             │
└─────────────────────────────────────┘
```

### 2. 关于对话框

**位置:** 帮助菜单 → "关于 JLab"

**UI 布局:**
```
┌─────────────────────────────────────┐
│  关于 JLab                        × │
├─────────────────────────────────────┤
│                                     │
│         [JLab 图标/Logo]            │
│                                     │
│         JLab 图像标注工具            │
│         版本 0.6.0                  │
│                                     │
│  JLab 是一款专业的 2D 目标检测      │
│  与属性分类的图像标注工具...        │
│                                     │
│  许可证: MIT OR Apache-2.0          │
│  🌐 https://github.com/...          │
│                                     │
│        [确定]                       │
└─────────────────────────────────────┘
```

### 3. 菜单结构调整

**文件菜单（新增"选项"）：**
```
打开项目...     Ctrl+O
保存            Ctrl+S
自动保存        ☑
─────────────────
导入           >
导出           >
最近项目       >
─────────────────
选项...         ← 新增
─────────────────
关闭项目
退出            Ctrl+Q
```

**帮助菜单（重构）：**
```
帮助            F1          ← 用户手册
检查更新                    ← 新增
─────────────────
关于 JLab                  ← 新增
```

**移除内容:**
- 语言子菜单（移到选项对话框）
- 快捷键设置（移到选项对话框）
- 快捷键说明内容（不再需要）

---

## 数据结构

### OptionsDialogState
```rust
pub struct OptionsDialogState {
    pub show: bool,
    pub active_tab: OptionsTab,
    pub selected_language: Language,
    pub auto_save_enabled: bool,
    pub shortcut_editor: Option<ShortcutEditorState>,
}

pub enum OptionsTab {
    General,   // 常规
    Shortcuts, // 快捷键
}
```

### AboutDialogState
```rust
pub struct AboutDialogState {
    pub show: bool,
}
```

---

## 翻译条目

### menu 节点
```json
"file_options": "选项...",
"help_help": "帮助",
"help_check_updates": "检查更新...",
"help_about": "关于 JLab"
```

### hint 节点
```json
"hint.file_options": "打开应用程序选项",
"hint.help_help": "查看用户手册",
"hint.help_check_updates": "检查是否有新版本",
"hint.help_about": "关于本应用程序"
```

### options 节点（新增）
```json
"options": {
  "title": "选项",
  "tab_general": "常规",
  "tab_shortcuts": "快捷键",
  "language": "界面语言:",
  "auto_save": "自动保存",
  "ok": "确定",
  "cancel": "取消"
}
```

### about 节点（新增）
```json
"about": {
  "title": "关于 JLab",
  "description": "JLab 是一款专业的 2D 目标检测与属性分类的图像标注工具。",
  "features": "支持多边形标注、ROI 标注，以及 YOLO、VOC、COCO 等多种格式导出。",
  "license": "许可证: MIT OR Apache-2.0",
  "version": "版本",
  "website": "项目主页"
}
```

---

## 涉及文件

### 新增文件
- `lab-gui/src/app/options_dialog.rs` - 选项对话框实现
- `lab-gui/src/app/about_dialog.rs` - 关于对话框实现

### 修改文件
- `lab-gui/src/app/menu.rs` - 菜单结构调整
- `lab-gui/src/app.rs` - 集成新对话框
- `lab-gui/src/state.rs` - 添加对话框状态
- `lab-gui/locales/zh-CN.json` - 中文翻译
- `lab-gui/locales/en-US.json` - 英文翻译

---

## 验收标准

1. ✅ 文件菜单有"选项..."条目
2. ✅ 选项对话框包含常规和快捷键两个标签页
3. ✅ 帮助菜单只有：帮助、检查更新、关于
4. ✅ 关于对话框显示版本号和描述
5. ✅ 语言设置从帮助菜单移到选项对话框
6. ✅ 快捷键设置从帮助菜单移到选项对话框
7. ✅ 所有设置变更立即生效
8. ✅ 中英文翻译完整
